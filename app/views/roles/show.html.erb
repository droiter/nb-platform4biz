<h2><i class="glyphicon glyphicon-info-sign"> Show Role</i></h2>

<p id="notice"><%= notice %></p>

<div class='tab'>

<table class="table table-hover">
	<caption>Role Information</caption>

	<tr>
		<th>KEY</th>
		<th>VALUE</th>
	</tr>

	<tr>
		<td>Name</td>
		<td><%= @role.name %></td>
	</tr>

	<tr>
		<td>Description</td>
		<td><%= @role.description %></td>
	</tr>

	<tr>
		<td>Status</td>
		<td><%= @role.status.name %></td>
	</tr>
</table>

</div>

<%= link_to 'Edit', edit_role_path(@role) %> |
<%= link_to 'Back', roles_path %>
