<%= form_for(@statement) do |f| %>
  <% if @statement.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@statement.errors.count, "error") %> prohibited this statement from being saved:</h2>

      <ul>
      <% @statement.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

	<table class="table table-hover">
		<tr>
			<th>KEY</th>
			<th>VALUE</th>
		</tr>

		<tr>
			<td>Type</td>
			<td><%= f.number_field :statement_type %></td>
		</tr>

		<tr>
			<td>Calendar</td>
			<td><%= f.number_field :calendar_id %></td>
		</tr>

		<tr>
			<td>Created at</td>
			<td><%= f.datetime_select :created_at %></td>
		</tr>

		<tr>
			<td>Updated at</td>
			<td><%= f.datetime_select :updated_at %></td>
		</tr>
	</table>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
